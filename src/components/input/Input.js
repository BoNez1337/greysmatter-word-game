import React from 'react';
import './Input.css';

const Input = (props) => {
    let anagram = props.anagram
    return (
        <div className="gm-input">
            <input type="text" />
        </div>
    )
}

export default Input
